# ─────────────────────────────────────────────────────────────────────────────
# MEMBRA QR Gateway — Kubernetes Secrets Template
#
# IMPORTANT: This file contains PLACEHOLDER values only.
# Replace all base64 values with real secrets before applying.
#
# To base64-encode a value:
#   echo -n "your-secret-value" | base64
#
# To apply:
#   kubectl apply -f secrets.yaml -n membra
#
# NEVER commit a secrets.yaml with real values to git.
# Use sealed-secrets, external-secrets-operator, or Vault for production.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: membra-secrets
  namespace: membra
  labels:
    app.kubernetes.io/name: membra
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # echo -n "your-64-hex-char-jwt-secret" | base64
  jwt-secret: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24tbWluLTMyLWNoYXJz

  # echo -n "your-64-hex-char-api-key-salt" | base64
  api-key-salt: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=

  # echo -n "your-admin-api-key" | base64
  admin-api-key: Y2hhbmdlLW1l

  # echo -n "sk_live_..." | base64
  stripe-secret-key: c2tfbGl2ZV9SRVBMQUNFX01F

  # echo -n "whsec_..." | base64
  stripe-webhook-secret: d2hzZWNfUkVQTEFDRV9NRQ==

  # echo -n "your-membra-event-secret" | base64
  membra-event-secret: UkVQTEFDRV9NRQ==
